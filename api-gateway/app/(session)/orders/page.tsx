// app/(session)/orders/page.tsx
import {} from "@/lib/publisher";

export default async function OrdersPage() {
  // Esto puede ser una función que haga fetch a tu API de Redis o a tu DB
//   const messages;// = await getOrders();

//   return (
//     <ul>
//       {messages.map((m, i) => (
//         <li key={i}>
//           {m.channel}: {m.message}
//         </li>
//       ))}
//     </ul>
//   );
}

